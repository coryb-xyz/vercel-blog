apiVersion: v1
kind: Service
metadata:
  name: {{ include "vercel-blog.fullname" . }}
  labels:
    {{- include "vercel-blog.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 3000
      protocol: TCP
      name: http
  selector:
    {{- include "vercel-blog.selectorLabels" . | nindent 4 }}
